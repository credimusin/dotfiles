# LocalSend Window Configuration
# Run in floating mode, size 800x600, positioned in the center of the screen
for_window [app_id="(?i)localsend"] floating enable, resize set 800 600, move position center
for_window [class="(?i)localsend"] floating enable, resize set 800 600, move position center
